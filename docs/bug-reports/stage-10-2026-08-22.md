# Stage 10 Sonrası Bug Avcılığı Raporu — 2026-08-22

**Ajan:** 11 (Bug Avcısı)
**Kapsam:** Tüm 10 yapım aşaması tamamlandıktan sonra baştan sona (fresh-eyes) kod tabanı denetimi — yalnızca son stage'in diff'i değil, tüm `apps/web`, `apps/api`, `apps/ai-service`, `packages/*` ve `supabase/migrations`.

## Genel Özet

Derleme (`build`), statik analiz (`lint`) ve test paketlerinin tamamı **hatasız/yeşil** durumda: `pnpm --filter web build`, `pnpm --filter api build`, `eslint` (her iki uygulama), `ruff`, `black --check`, `mypy` ve tüm test paketleri (`vitest`, `jest`, `pytest`) sıfır hata ile tamamlandı. Üç servis de (web/api/ai-service) hiçbir Supabase/Redis/Sentry ortam değişkeni tanımlanmadan gerçekten ayağa kaldırıldı ve temel uç noktaları (`/`, `/health`, `/login`) 200 döndürdü — "Supabase olmadan da çalışır" iddiası doğrulandı. Repo genelinde gerçek/sızdırılmış bir gizli anahtar, token veya şifre bulunamadı; tüm `.env.example` dosyaları placeholder.

Bununla birlikte, derleme/lint/test yeşil olması runtime doğruluğunu garanti etmiyor: kod okuma ve manuel izleme ile **17 bulgu** tespit edildi (bunların hiçbiri test paketleri tarafından yakalanmıyor, çünkü ilgili riskli dosyaların (PlayerEngine, usePracticeSession, tus middleware) hiçbir testi yok). Hiçbir bulgu "Kritik" (veri kaybı/çökme/başka kullanıcının verisine erişim) seviyesinde değil — tüm sahiplik kontrolleri (tracks/jobs/transform controller'ları, RLS politikaları) doğru kurulmuş. Ancak **5 Yüksek** öncelikli bulgu var: depolama kotası yarış durumu + hatalı boyut hesaplama, `/tracks/:id` sayfasında iş durumunun hiç canlı güncellenmemesi, prova modunda duraklatma olayının bazen hiç yayınlanmaması, ve hesap silmenin Supabase Storage'daki gerçek ses dosyalarını temizlememesi.

**Bulgu sayıları:** Kritik: 0 · Yüksek: 5 · Orta: 9 · Düşük: 3 (toplam 17)

---

## Test Sonuçları

| Adım | Komut | Sonuç |
|---|---|---|
| Web build | `pnpm --filter web build` | ✅ Başarılı — "Compiled successfully in 4.0s", TypeScript kontrolü 0 hata, 19 rota üretildi |
| API build | `pnpm --filter api build` | ✅ Başarılı — `nest build`, exit 0 |
| Web lint | `pnpm --filter web lint` (eslint) | ✅ 0 hata/uyarı |
| API lint | `pnpm --filter api lint` (eslint --fix) | ✅ 0 hata/uyarı |
| AI ruff | `ruff check .` | ✅ "All checks passed!" |
| AI black | `black --check .` | ✅ "All done! 18 files would be left unchanged." |
| AI mypy | `mypy .` | ✅ "Success: no issues found in 18 source files" |
| Web test | `pnpm --filter web test` (vitest) | ✅ 2 test dosyası, 6/6 test geçti (yalnızca `chords.ts`/`format.ts` saf fonksiyonları kapsıyor) |
| API test | `pnpm --filter api test` (jest) | ✅ 7 test suite, 30/30 test geçti |
| AI pytest | `pytest` (apps/ai-service, `.venv`) | ✅ 20/20 test geçti; 33 `DeprecationWarning` (`asyncio.iscoroutinefunction`, Python 3.16'da kaldırılacak) — bkz. Düşük #17 |
| Boot smoke — API | `node dist/main.js` (env değişkeni yok) | ✅ Başlatıldı, `GET /` → 200, `REDIS_URL` yokken worker düzgün şekilde "devre dışı" uyarısı verip devam etti |
| Boot smoke — AI servis | `uvicorn app.main:app` (env değişkeni yok) | ✅ Başlatıldı, `GET /health` → 200 |
| Boot smoke — Web | `next start` (env değişkeni yok) | ✅ Başlatıldı, `GET /` → 200, `GET /login` → 200 |
| Gizli anahtar taraması | repo geneli regex grep (JWT/AWS/Google/PEM anahtar desenleri) + `.env.example` içerik incelemesi | ✅ Gerçek/sızdırılmış kimlik bilgisi bulunamadı; tüm `.env.example` placeholder |
| TODO/FIXME taraması | `apps/web`, `apps/api`, `apps/ai-service` kaynak kodu | ✅ Bulunamadı |

---

## Bulgular

### Yüksek (High)

| Önem (Severity) | Konum (Location: file:line) | Açıklama (Description) | Önerilen Düzeltme (Suggested Fix) | Durum (Status) |
|---|---|---|---|---|
| Yüksek | `apps/api/src/uploads/tus-upload.middleware.ts:126-159` (`enforceStorageQuota`), `apps/api/src/uploads/upload-validation.ts:43-49` (`wouldExceedStorageQuota`) | Depolama kotası kontrolü klasik TOCTOU (time-of-check to time-of-use) yarış durumuna açık: mevcut kullanım bir `SELECT SUM(size_bytes)` ile okunuyor, kontrol ediliyor, ve yalnızca çok sonra (FFmpeg normalizasyonu bittikten sonra) yeni `tracks` satırı `INSERT` ediliyor. Aynı kullanıcının iki eşzamanlı yüklemesi aynı "mevcut kullanım" değerini okuyup ikisi de kontrolü geçebilir, kota toplamda aşılabilir. Stage 10 handoff'u da (`docs/handoffs/stage-10.md` satır 80) bunu Ajan 12 için açık bir soru olarak bırakmıştı — burada race'in gerçekten var olduğu doğrulandı. | Kota kontrolünü atomik hale getirin: ör. Postgres tarafında `SELECT ... FOR UPDATE` ile satır kilitleme, ya da bir advisory lock (`pg_advisory_xact_lock(hashtext(user_id))`) kullanıcı bazında eşzamanlı yüklemeleri serileştirsin; ya da kotayı bir Postgres trigger/constraint ile (ör. kullanıcı başına toplamı tutan bir sayaç satırı + `CHECK`) sunucu tarafında atomik uygulayın. | Açık |
| Yüksek | `apps/api/src/uploads/tus-upload.middleware.ts:183` ve `:216` | Kota kontrolü `enforceStorageQuota(userId, upload.size ?? 0, ...)` ham (genelde sıkıştırılmış, ör. mp3) yükleme boyutunu kullanıyor, ama gerçekte veritabanına yazılan `size_bytes: fileBuffer.length` FFmpeg ile **normalize edilmiş WAV/PCM** dosyasının boyutu — mp3'ten genelde çok daha büyük (10 kata kadar). Yani kota kontrolü sistematik olarak kullanıcının gerçek depolama ayak izini olduğundan çok daha düşük tahmin ediyor; kullanıcılar 2 GB kotayı kolayca ve tutarlı biçimde aşabilir. | Kota kontrolünü normalize edilmiş dosya yazıldıktan/boyutu bilindikten sonra (ya da normalize edilmiş boyutun tahmini bir katsayıyla — WAV/kaynak oranı — büyütülmüş haliyle) tekrar doğrulayın; ya da kotayı FFmpeg sonrası gerçek `fileBuffer.length` ile yeniden kontrol edip aşıyorsa reddedin. | Açık |
| Yüksek | `apps/web/app/tracks/[trackId]/page.tsx` (tamamı — Server Component, tek seferlik `fetchTrack`), `apps/web/components/player/PlayerView.tsx:21-51` | `jobs` tablosu Supabase Realtime yayınına eklenmiş (`supabase/migrations/0002_tracks_and_jobs.sql:47`, açıkça "İstemcinin Realtime üzerinden iş durumunu anlık takip edebilmesi için" yorumuyla) ama **hiçbir yerde** (`PlayerView`, track sayfası, başka bir hook) buna abone olunmuyor, hiçbir polling de yok (`setInterval`/`router.refresh` araması sıfır sonuç verdi). Kullanıcı yükleme sonrası `/tracks/{id}`'ye yönlendirildiğinde iş hâlâ "processing" ise sayfa sonsuza dek "Parça işleniyor…" gösterir; tamamlandığını görmek için kullanıcının sayfayı elle yenilemesi gerekir. Bu, PRD'nin bizzat tanımladığı ana kullanıcı akışını (yükle → işlensin → oynatıcıda kullan) kırıyor. | `PlayerView`'a (veya track sayfasına) `jobs` tablosuna `postgres_changes` ile bir Supabase Realtime aboneliği ekleyin (ör. `usePracticeSession`'daki gibi bir `useEffect` içinde), ya da en azından `job.status !== 'done'` iken birkaç saniyede bir `router.refresh()`/client-side polling yapın. | Açık |
| Yüksek | `apps/web/lib/practice/usePracticeSession.ts:155-174` | Host'un durumu yayınlayan efekt, `BROADCAST_THROTTLE_MS` (300ms) içinde yalnızca "leading edge" atlıyor — yayın atlanan son durumu daha sonra tekrar göndermek için bir zamanlayıcı (trailing-edge send) yok. `engine.currentTime` sürekli değiştiği için pratikte sorun az görülür, ama host **duraklattığında** (`isPlaying` değişimi) bu olay son yayından <300ms sonra gerçekleşirse yayın tamamen atlanır — ve duraklatıldığı için `currentTime` artık değişmeyeceğinden efekt bir daha asla tetiklenmez. Sonuç: takipçi cihazlar host durdurulduğunu **hiçbir zaman öğrenmez**, çalmaya devam eder. Bu, Özellik 8'in (Senkronize Prova Modu) temel doğruluğunu bozan gerçek bir bug. | Throttle'a bir "trailing call" ekleyin: atlanan her çağrıda bir `setTimeout` ile throttle penceresi bittiğinde en güncel durumu garantili gönderin (klasik throttle+trailing deseni), özellikle `isPlaying` değiştiğinde throttle'ı bypass edip anında gönderin. | Açık |
| Yüksek | `apps/api/src/users/users.controller.ts:24-31` (`deleteMe`) | Hesap silme yalnızca `supabase.admin.auth.admin.deleteUser(user.id)` çağırıyor; bu, `profiles`/`tracks`/`jobs`/`playlists` gibi Postgres satırlarını `ON DELETE CASCADE` ile temizler ama **Supabase Storage'daki gerçek ses dosyalarını (`tracks` bucket, `raw/{userId}/...`) hiç silmiyor** — Storage nesneleri Postgres satırlarına foreign-key ile bağlı değil, cascade bunları kapsamıyor. Kod yorumu bunu bizzat "KVKK/GDPR 'unutulma hakkı'" gerekliliği olarak tanımlıyor, ama kullanıcının gerçek ses kayıtları (potansiyel olarak tanımlayıcı/kişisel içerik) silme sonrası kalıcı olarak depoda kalmaya devam ediyor. | `deleteMe()` içinde kullanıcı silinmeden önce (veya silindikten hemen sonra, kullanıcının `tracks` satırlarından `storage_path`'leri toplayarak) `supabase.admin.storage.from('tracks').remove([...paths])` çağrısı ekleyin. | Açık |

### Orta (Medium)

| Önem (Severity) | Konum (Location: file:line) | Açıklama (Description) | Önerilen Düzeltme (Suggested Fix) | Durum (Status) |
|---|---|---|---|---|
| Orta | `apps/web/lib/practice/usePracticeSession.ts:64-124` (özellikle `await supabase.realtime.setAuth();` satırı ve sonrası) | `connect()` içinde `cancelled` bayrağı yalnızca `getUser()` sonrası kontrol ediliyor; `setAuth()` await'inden sonra tekrar kontrol edilmiyor. Efekt temizleme fonksiyonu bu iki await arasında çalışırsa (hızlı unmount/remount, React StrictMode çift-çalıştırma, veya hızlı `trackId` değişimi), kanal yine de oluşturulup abone olunur ve `channelRef.current`'a atanır — ama cleanup zaten çalışıp bitmiş olduğundan bu kanal hiçbir zaman `removeChannel` ile kapatılmaz. Sonuç: sızan bir Realtime WebSocket aboneliği. | `setAuth()` sonrasında da `if (cancelled) return;` kontrolü ekleyin; ya da `channelRef.current = channel` atamasından hemen önce tekrar kontrol edip cancelled ise `channel.unsubscribe()` çağırıp fonksiyondan çıkın. | Açık |
| Orta | `apps/web/lib/player/PlayerEngine.ts:104-111` (`syncStemsToWavesurfer`), `:219` (`replaceStemSources` içinde çağrısı) | `void audio.play()` hiçbir yerde `.catch()` edilmiyor. `HTMLMediaElement.play()`'in döndürdüğü promise, tarayıcı otomatik oynatma politikası engellediğinde veya `destroy()`/hızlı seek ile eşzamanlı çağrıldığında (`AbortError`) reddedilebilir — bu durumda yakalanmamış bir promise reddi (unhandled promise rejection) oluşur. | Her `audio.play()` çağrısına `.catch((err) => { /* logla veya sessizce yut */ })` ekleyin. | Açık |
| Orta | `apps/web/lib/player/PlayerEngine.ts:203-220` (`replaceStemSources`) | Yeni oluşturulan `Audio` elementine (`new Audio(url)`, henüz `loadedmetadata` tetiklenmemiş, `readyState === HAVE_NOTHING`) hemen `audio.currentTime = time` atanıyor. Tarayıcılar arası bu davranış güvenilir değildir — bazı tarayıcılarda metadata yüklenene kadar atama yok sayılır/sıfırlanır. Tempo/ton dönüşümü sırasında (bir stem oynatılırken değiştirildiğinde) stem, doğru senkron pozisyondan değil baştan (0'dan) çalmaya başlayabilir. | `audio.addEventListener('loadedmetadata', () => { audio.currentTime = time; ... })` deseniyle `currentTime` atamasını metadata yüklenene kadar erteleyin. | Açık |
| Orta | `apps/web/app/playlists/[playlistId]/page.tsx:9-15,55-60` (`UserTrack.artist`/`.tags`), `apps/api/src/tracks/tracks.controller.ts:43-56` (`listTracks`) | `GET /tracks` yalnızca `id, title, duration_seconds, created_at` seçiyor — `artist`/`tags` sütunları (`supabase/migrations/0004_playlists.sql` ile eklendi) hiç seçilmiyor. Playlist detay sayfasındaki "Parça Ekle" arama kutusu bu alanlara göre filtreleme yapmayı bekliyor (`t.artist`, `t.tags`) ama backend bunları hiç döndürmediği için arama sanatçı/etikete göre asla eşleşmiyor (sessizce, hatasız — sadece işe yaramıyor). Ayrıca `GET /tracks` snake_case (`duration_seconds`, `created_at`) döndürürken `GET /tracks/:id` camelCase (`durationSeconds`, `createdAt`) döndürüyor — aynı API içinde tutarsız sözleşme. | `listTracks`'ın `select(...)` listesine `artist, tags`'i ekleyin ve yanıtı `GET /tracks/:id` ile tutarlı camelCase'e çevirin (`durationSeconds`, `createdAt`). | Açık |
| Orta | `packages/shared-types/src/index.ts` (tüm dosya), `apps/web/package.json`, `apps/api/package.json` | `@woodshed/shared-types` paketi hiçbir yerde gerçek bir `import` ile kullanılmıyor (yalnızca `apps/web/next.config.ts`'in `transpilePackages` listesinde ve bir yorumda geçiyor) ve ne `apps/web/package.json` ne de `apps/api/package.json`'da bağımlılık olarak tanımlı — tamamen "kullanılmayan/bağlantısız" bir paket. Bunun sonucu olarak `apps/web/lib/player/api.ts`'teki `TrackDetail` ve `apps/api/src/tracks/tracks.controller.ts`'teki `JobRow`/dönüş şekli gibi tipler elle, birbirinden bağımsız olarak kopyalanmış durumda — derleyici garantisi olmadan gelecekte sessizce birbirinden sapabilirler (yukarıdaki `artist`/`tags` ve casing tutarsızlığı bunun canlı bir örneği). | Ya `@woodshed/shared-types`'ı her iki uygulamaya da gerçek bir workspace bağımlılığı olarak ekleyip DTO'ları/yanıt tiplerini oradan import edin, ya da paket kullanılmayacaksa kaldırıp "tek gerçek kaynak" iddiasını dokümantasyondan çıkarın. | Açık |
| Orta | `apps/api/src/uploads/tus-upload.middleware.ts:161-257` (`handleUploadFinish`, özellikle `:228-250`) | Storage upload ve `tracks` satırı eklendikten SONRA (`jobs` insert veya `queue.enqueue`) başarısız olursa, hiçbir telafi edici (compensating) temizlik yok: Storage'da yüklü dosya ve DB'de sahipsiz bir `tracks` satırı kalıcı olarak kalır (hiçbir `jobs` satırı/işlenme olmadan), kullanıcının kotasından sonsuza dek düşülmüş olur ama kullanıcı bunu hiçbir arayüzde göremez/silemez. | `try/catch` bloğunu genişletip, `jobError`/`queue.enqueue` hatasında daha önce oluşturulan `track`/Storage nesnesini geri alın (silin) veya en azından `jobs`'a `status: 'failed'` ile bir kayıt açıp kullanıcıya görünür kılın. | Açık |
| Orta | `apps/api/src/queue/track-processing.worker.ts:122-150` (`callAiService`), `apps/api/src/queue/track-processing.worker.ts:67` (`concurrency`) | AI servisine yapılan `fetch` çağrılarında hiçbir timeout/`AbortController` yok (bilinçli bir tasarım — yorum satırında açıklanmış) ama `MAX_CONCURRENT_JOBS` varsayılanı 1. AI servisi (Demucs/ffmpeg) bir istekte askıda kalırsa (network sorunu, kilitlenme), o worker slotu süresiz meşgul kalır ve kuyruktaki **tüm** diğer işler de durur — tek bir yavaş/asılı istek tüm işleme hattını kilitler. | AI servisine yapılan her çağrıya makul bir üst sınır (ör. 30-60 dakika) ile `AbortController` tabanlı bir timeout ekleyin; aşılırsa işi `failed` işaretleyip devam edin. | Açık |
| Orta | `apps/api/src/auth/supabase-auth.guard.ts:38`, `apps/api/src/uploads/tus-upload.middleware.ts:114` (`jwt.verify(token, secret)`) | Her iki `jwt.verify()` çağrısı da `algorithms` seçeneğini elle sabitlemiyor (allowlist yok) — savunma-derinliği (defense-in-depth) açısından en iyi pratik değil; statik güvenlik tarayıcıları (Semgrep/CodeQL) bunu tipik olarak işaretler. Şu an simetrik (HS256) bir sır kullanıldığından pratik bir algoritma karışıklığı saldırısı yolu görünmüyor, ama açıkça sabitlenmemesi gelecekte (ör. yanlışlıkla asimetrik anahtara geçiş) risk oluşturur. | Her iki çağrıya da `jwt.verify(token, secret, { algorithms: ['HS256'] })` ekleyin. | Açık |
| Orta | `apps/web/lib/player/PlayerEngine.ts`, `apps/web/lib/player/usePlayerEngine.ts`, `apps/web/lib/practice/usePracticeSession.ts`, `apps/web/lib/playlists/usePlaylistTracks.ts`, `apps/web/app/upload/page.tsx` (tümü test kapsamı dışında) | `apps/web`'in test paketi yalnızca 2 dosyadan (`chords.test.ts`, `format.test.ts` — iki saf yardımcı fonksiyon) oluşuyor, 6 test. Görevin özellikle riskli olarak işaretlediği çoklu-ses-elementi senkronizasyonu (`PlayerEngine`), Realtime kanal yaşam döngüsü (`usePracticeSession`), tus yükleme akışı ve playlist hook'larının **hiçbiri test edilmiyor** — bu raporda bulunan pek çok bug (throttle drop, channel leak, unhandled rejection) sıradan bir test çalıştırmasıyla hiç yakalanamazdı. | En azından `usePracticeSession`'ın throttle/cancel mantığı ve `PlayerEngine`'in mixer/loop/drift-correction davranışı için sahte (fake) zamanlayıcılar ve mock `HTMLMediaElement`/Supabase Realtime istemcisi kullanan birim testleri ekleyin. | Açık |

### Düşük (Low)

| Önem (Severity) | Konum (Location: file:line) | Açıklama (Description) | Önerilen Düzeltme (Suggested Fix) | Durum (Status) |
|---|---|---|---|---|
| Düşük | `apps/web/lib/player/usePlayerEngine.ts:34-63` | Efekt bağımlılık dizisi yalnızca `[options.visualUrl, containerRef]` — ama hemen üstündeki yorum "Konteyner ve stem URL'leri değiştiğinde motor sıfırdan kurulmalı" diyor, gerçekte `options.stemUrls` değişse bile motor yeniden kurulmuyor (stem güncellemeleri şu an yalnızca `PlayerView.tsx`'teki açık `replaceStemSources()` çağrısıyla yapılıyor). Şu anki tek kullanım yeri bu yüzden pratikte etkilenmiyor, ama yorum yanıltıcı ve gelecekteki bir değişiklik (stemUrls'i prop olarak değiştirip motorun otomatik güncelleneceğini varsayan biri) sessizce bozulabilir. | Yorumu gerçek davranışla eşleştirin: "stem URL'leri `replaceStemSources` ile ayrıca yönetilir, bu efekt yalnızca konteyner/görsel URL değiştiğinde tetiklenir." | Açık |
| Düşük | `apps/web/lib/player/Metronome.ts` (tüm sınıf) + `apps/web/lib/player/usePlayerEngine.ts:65-74` | `options.bpm` değiştiğinde ilk efekt, `Metronome`'u (ve içindeki yeni `AudioContext`'i) tamamen yeniden oluşturuyor. Metronom o an çalıyorsa (`start()` çağrılmışsa), yeni nesne başlatılmamış (`timerId: null`) durumda gelir — metronom sessizce durur ama `metronomeOn` React state'i `true` kalır, arayüz "Açık" göstermeye devam eder. Şu anki uygulamada `bpm` yalnızca `track.job?.bpm`'den geliyor ve component ömrü boyunca pratik olarak sabit olduğundan bu yol nadiren tetikleniyor, ama kod olarak yanlış. | Bpm değiştiğinde tüm nesneyi yeniden kurmak yerine yalnızca `metronomeRef.current?.setBpm(bpm)` çağırın (zaten ikinci efekt bunu yapıyor); metronomun çalışıp çalışmadığı durumunu (`start()`/`stop()`) `bpm` değişiminden bağımsız tutun. | Açık |
| Düşük | `apps/ai-service/.venv` (Python 3.14.6), `apps/ai-service/pyproject.toml:3` (`target-version = "py312"`) | Kurulu geliştirme sanal ortamı Python 3.14.6 kullanıyor, ama `pyproject.toml` açıkça `python_version = "3.12"`/`target-version = "py312"` hedefliyor. Bu uyumsuzluk nedeniyle `pytest` çalıştırması 33 adet `DeprecationWarning` (`asyncio.iscoroutinefunction`, Python 3.16'da kaldırılacak — FastAPI/Starlette'in kendi iç kullanımından) üretiyor. Şu an testler geçiyor ama CI/geliştirme ortamı pyproject'in hedeflediği sürümle eşleşmiyor, ve bu uyarılar üretimde farklı bir Python sürümüyle davranış farkına işaret edebilir. | `.venv`'i `pyproject.toml`'da belirtilen Python 3.12 ile yeniden oluşturun (ör. `uv venv --python 3.12` veya `py -3.12 -m venv .venv`), ya da hedef sürümü gerçekten kullanılan sürümle güncelleyin. | Açık |

---

## Notlar

- Sahiplik/erişim kontrolleri (`tracks.controller.ts`, `jobs.controller.ts`, `transform.controller.ts`, `supabase/migrations/0005_*.sql`'deki Realtime Authorization RLS'i) incelendi ve doğru kurulmuş bulundu — bu alanlarda bir bulgu raporlanmadı.
- `packages/ui` ve `apps/api`'nin diğer modülleri (ffmpeg, auth guard testi vb.) yüzeysel olarak incelendi, belirgin bir ek bug bulunmadı.
- Bu rapor salt tespit amaçlıdır; hiçbir kaynak dosya değiştirilmemiştir.
